export interface GridProps {
    templateRow?: string;
    templateColumn?: string;
    gap?: string;
    columnGap?: string;
    rowGap?: string;
    height?: string;
    margin?: string;
    children?: React.ReactNode;
}