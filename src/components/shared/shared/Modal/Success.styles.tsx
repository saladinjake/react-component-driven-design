import styled from "styled-components";
import { ModalProps } from "./Modal";

const StyledSuccessModal = styled.div<ModalProps>`
  .content {
    margin: 20px 15px;
  }
`;

export default StyledSuccessModal;